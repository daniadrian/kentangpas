# Database Configuration
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://petani:petani123@localhost:5432/kalkulator_db?schema=public"

# Server Configuration
PORT=4000
NODE_ENV=production

# CORS Configuration (optional) — tambah domain ekstra tanpa edit kode
# Pisahkan dengan koma, tanpa spasi
# ALLOWED_ORIGINS=https://example.com,http://localhost:8080
