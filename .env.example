# Database Configuration
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://petani:petani123@localhost:5432/kalkulator_db?schema=public"

# Server Configuration
PORT=4000
NODE_ENV=production

# CORS Configuration (optional, sudah di-hardcode di index.js)
# ALLOWED_ORIGINS=http://bibitku.filkom.ub.ac.id,https://bibitku.filkom.ub.ac.id
